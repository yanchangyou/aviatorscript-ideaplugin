while (socket =accept(server)) != nil {
}


fn mymap(seq, fn) {
                  reduce(seq,
                            lambda(c, e) ->
                            seq.add(c, fn(e))
                                end,
seq.list())
}
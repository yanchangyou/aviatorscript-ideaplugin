//

plugins {
    id "org.jetbrains.intellij" version "0.6.1"
}

group 'lang.plugin.idea'
version '0.2.2-SNAPSHOT'

repositories {
    mavenLocal()
    maven {
        url "http://maven.aliyun.com/nexus/content/groups/public"
    }
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

sourceSets {

    main {
        java {
            srcDir 'src/main/java'
            srcDir 'src/main/gen-bnf'
            srcDir 'src/main/gen-flex'
        }
        resources {
            srcDir 'src/main/resource'
        }
    }

    test {
        java {
            srcDir 'src/test/java'
        }
        resources {
            srcDir 'src/test/resource'
        }
    }
}

// See https://github.com/JetBrains/gradle-intellij-plugin/
intellij {
    version '2020.2.3'
    plugins = ['java']
}

patchPluginXml {
    changeNotes """
      update list of AviatorScript idea plugin<br/>
     <br/> 
        v0.1<br/>
        1, ADD: add a highlight function for AviatorScript<br/>
        <br/>
        v0.2<br/>
        1, FIX: let, try, if, for lambda statement<br/>
        2, ADD: =~<br/>
        3, FIX: param referer<br/>
        4, FIX: regStatement<br/>
        <br/>
        v0.2.1<br/>
        1, FIX: plugin verification <br/> 
        <br/>
        v0.2.2<br/>
        1, IMPROVE: remove the java module for more platform<br/>
        <br/>
    """
}
